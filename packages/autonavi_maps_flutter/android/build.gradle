group 'com.autonavi.maps.flutter'
version '1.0'

apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

android {
    if (project.android.hasProperty("namespace")) {
        namespace 'com.autonavi.maps.flutter'
    }

    compileSdk 34

    defaultConfig {
        minSdk 21
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }
}

dependencies {
    // Pinned to exact version — upgrade intentionally and review the official changelog first.
    // Changelog: https://lbs.amap.com/api/android-sdk/guide/create-project/android-studio-create-project
    implementation 'com.amap.api:3dmap:10.0.600'
}
