/// Enumerates the different map types supported by AutoNavi.
enum MapType {
  /// Normal map (default).
  normal,

  /// Satellite imagery.
  satellite,

  /// Night mode map.
  night,

  /// Navigation map optimized for driving.
  navi,

  /// Bus/transit map.
  bus,
}
